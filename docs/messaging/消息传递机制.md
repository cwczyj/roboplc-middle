# 消息传递机制

> RoboPLC 使用 Hub 消息总线实现 Worker 之间的消息传递。本文档详细说明消息类型、传递流程和设计决策。

## 🔄 消息类型

### DeviceControl
设备控制请求消息，用于控制 Modbus 设备。

**字段：**
```rust
pub struct DeviceControl {
    pub device_id: String,          // 目标设备 ID
    pub operation: Operation,       // 操作类型
    pub params: JsonValue,          // 操作参数
    pub correlation_id: u64,        // 请求 ID，用于匹配响应
    pub respond_to: Option<Sender<DeviceResponseData>>, // 直接响应通道
}
```

**操作类型：**
- `ReadSignalGroup`：读取信号组
- `WriteSignalGroup`：写入信号组
- `GetStatus`：查询设备状态

### DeviceResponse
设备控制响应消息，包含操作结果。

**字段：**
```rust
pub struct DeviceResponse {
    pub device_id: String,          // 目标设备 ID
    pub success: bool,              // 操作是否成功
    pub data: JsonValue,            // 操作结果数据
    pub error: Option<String>,      // 错误信息（如果有）
    pub correlation_id: u64,        // 请求 ID，匹配原始请求
}
```

### DeviceResponseData
设备响应数据类型别名，用于直接响应机制。

```rust
pub type DeviceResponseData = (bool, JsonValue, Option<String>);
// 元组结构：(success, data, error)
```

### ConfigUpdate
配置更新通知消息，由 ConfigLoader 发送。

**字段：**
```rust
pub struct ConfigUpdate {
    pub config: String,  // JSON 格式的配置内容
}
```

### DeviceHeartbeat
设备心跳消息，保持连接活跃状态。

**字段：**
```rust
pub struct DeviceHeartbeat {
    pub device_id: String,       // 设备 ID
    pub timestamp_ms: u64,       // 时间戳（毫秒）
    pub latency_us: u64,         // 延迟（微秒）
}
```

### TimeoutCleanup
超时清理消息，由 RpcWorker 发送给 DeviceManager，用于清理超时请求。

**字段：**
```rust
pub struct TimeoutCleanup {
    pub correlation_id: u64,     // 超时请求的关联 ID
}
```

### SystemStatus
系统状态查询消息，由 HttpWorker 发送。

**字段：**
```rust
pub struct SystemStatus {
    pub requester: String,                           // 请求者标识
    pub respond_to: Sender<SystemStatusResponse>,    // 响应通道
}
```

### SystemStatusResponse
系统状态响应结构体。

**字段：**
```rust
pub struct SystemStatusResponse {
    pub devices_count: u32,      // 设备总数
    pub system_healthy: bool,    // 系统健康状态
    pub uptime_secs: u64,        // 运行时间（秒）
}
```

## 📡 消息传递流程

### 1. RPC Worker → Hub

```
RPC Worker (JSON-RPC Server)
    │
    ├── 接收 JSON-RPC 请求
    │
    ├── 解析请求并生成 DeviceControlRequest
    │
    ├── 生成唯一 correlation_id（原子递增计数器）
    │
    ├── 创建 oneshot 通道用于接收响应
    │
    └── 通过内部 mpsc 通道发送 DeviceControlRequest
    │
    └── Bridge 任务将请求转换为 Message::DeviceControl
    │
    └── 通过 Hub 发送 DeviceControl 消息
    │
    └── 等待 oneshot 通道响应（带超时）
    │
    └── 超时时发送 TimeoutCleanup 消息
    │
    └── 将结果转换为 JSON-RPC 响应返回客户端
```

**关键点：**
- **双通道机制**：RpcWorker 使用 `mpsc` 通道在 RpcHandler 和主循环间传递请求，使用 `oneshot` 通道接收响应
- **直接响应**：`respond_to` 字段包含 oneshot 发送端，允许直接响应而无需 Hub 路由
- **超时处理**：请求超时时发送 `TimeoutCleanup` 消息清理 `pending_requests`

### 2. Hub 路由

```
Hub
    │
    ├── 接收来自 RPC Worker 的 DeviceControl
    │
    ├── DeviceManager 接收并路由
    │   │
    │   ├── 使用 worker_map 查找目标 ModbusWorker
    │   │
    │   ├── 存储 respond_to 到 pending_requests（如果存在）
    │   │
    │   └── 通过 Hub 转发 DeviceControl 到目标 Worker
    │
    │
    └── ModbusWorker 处理请求
    │
    │   └── 发送 DeviceResponse 到 Hub
    │
    │
    └── DeviceManager 接收 DeviceResponse
    │
    │   ├── 通过 correlation_id 查找 pending_requests
    │   │
    │   ├── 如果找到，通过 respond_to 发送响应数据
    │   │
    │   └── 移除 pending_requests 中的条目
    │
    │
    └── 如果未找到，记录警告日志
```

**关键点：**
- **worker_map**：DeviceManager 维护设备 ID → Worker 名称的映射
- **pending_requests**：存储待处理请求的 `respond_to` 通道
- **直接响应**：使用 `respond_to` 通道发送响应，避免 Hub 路由开销

### 3. TimeoutCleanup 处理

```
RpcWorker 检测到请求超时
    │
    └── 发送 Message::TimeoutCleanup { correlation_id }
    │
    └── DeviceManager 接收
    │
    └── 从 pending_requests 中移除对应条目
    │
    └── 记录清理日志
```

### 4. Hub 消息优先级

Hub 使用 `event_matches!()` 宏选择特定消息类型：

```rust
// DeviceManager 订阅
event_matches!(
    Message::DeviceControl { .. } | Message::DeviceResponse { .. }
)

// ModbusWorker 订阅
event_matches!(Message::DeviceControl { .. })

// LatencyMonitor 订阅
event_matches!(Message::DeviceHeartbeat { .. })
```

**优先级规则：**
1. `DeviceHeartbeat` 最高优先级（保活连接）
2. `ConfigUpdate` 高优先级（配置热更新）
3. `DeviceControl` / `DeviceResponse` 正常优先级

## 📊 请求-响应生命周期

### 请求创建

```
1. RPC Worker 收到 JSON-RPC 请求
2. 生成 correlation_id（原子递增计数器）
3. 创建 oneshot 通道 (tx, rx)
4. 构建 DeviceControlRequest { ..., respond_to: tx }
5. 通过 mpsc 通道发送到 bridge 任务
6. Bridge 创建 Message::DeviceControl 并发送到 Hub
```

### 请求处理

```
1. Hub 投递 DeviceControl 到 DeviceManager
2. DeviceManager 查找目标 ModbusWorker
3. 存储 respond_to 到 pending_requests（如果存在）
4. 通过 Hub 转发到 ModbusWorker
5. ModbusWorker 执行操作
```

### 响应处理

```
1. ModbusWorker 完成操作
2. 发送 DeviceResponse 到 Hub
3. DeviceManager 接收 DeviceResponse
4. 通过 correlation_id 匹配 pending_requests
5. 通过 respond_to.send() 发送响应给原始请求者
6. 移除 pending_requests 中的条目
```

### 超时处理

```
1. RpcWorker bridge 任务检测到 oneshot::recv 超时
2. 发送 Message::TimeoutCleanup { correlation_id } 到 Hub
3. DeviceManager 接收并从 pending_requests 移除条目
4. 记录超时警告日志
```

### 错误处理

```
1. 操作失败时在 DeviceResponse 中设置 error 字段
2. DeviceManager 通过 respond_to 发送错误响应
3. 记录详细的错误日志
```

## 🎯 设计模式

### 1. 直接响应机制
使用 `respond_to: Option<Sender<DeviceResponseData>>` 实现直接响应，避免 Hub 路由开销。

### 2. 非阻塞设计
- RpcWorker 使用 tokio 异步运行时
- 使用 `tokio::select!` 实现并发处理
- 使用 `oneshot` 通道等待响应

### 3. 背压控制
ModbusWorker 的 `max_concurrent_ops` 配置限制最大并发操作数。

### 4. 优雅降级
连接失败时使用指数退避策略，避免重连风暴。

### 5. 状态机
ModbusWorker 维护连接状态、心跳时间、操作统计。

## 🔧 配置管理

### worker_map 初始化
DeviceManager 从配置加载设备时创建 worker_map：

```rust
for device in &config.devices {
    worker_map.insert(device.id.clone(), format!("modbus_worker_{}", device.id));
}
```

### 配置热更新流程

```
1. ConfigLoader 检测到配置文件变化
2. 解析新配置（JSON 差异）
3. 通过 Hub 广播 ConfigUpdate 消息
4. ConfigUpdater 接收并应用更新
   - 更新设备列表（添加/移除）
   - 更新 device_states
   - 更新 worker_map
   - 必要时重启 ModbusWorker
```

## 📚 并发安全

### Hub 消息投递
Hub 使用非阻塞投递（try_send），高优先级消息不会被阻塞。

### Worker 消息处理
每个 Worker 的 run() 方法顺序处理消息：
1. 验证消息类型和来源
2. 执行相应操作
3. 发送响应

## 🧪 测试策略

### 单元测试
每个结构体和关键函数都有单元测试。

### 集成测试
tests/e2e_tests.rs 包含端到端测试：
- RPC 请求 → Hub 路由 → ModbusWorker 执行 → 响应返回

### Mock 测试
tests/mock_modbus.rs 提供 Modbus 设备的 Mock 实现。

## 📝 相关文档

- [架构概览](../architecture/架构概览.md)
- [HTTP API](../http-api/http-api.md)
- [配置管理](../configuration/配置管理.md)
- [Worker 模块](../workers/worker模块.md)
